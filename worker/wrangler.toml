name = "p2p-clinic-signaling"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# KV for share codes and peer announcements (auto-expire)
[[kv_namespaces]]
binding = "KV"
id = "p2p-clinic-kv"
preview_id = "p2p-clinic-kv-preview"

# Durable Objects for WebSocket signaling (optional)
[[durable_objects.bindings]]
name = "ROOM"
class_name = "Room"

[[migrations]]
tag = "v1"
new_classes = ["Room"]
